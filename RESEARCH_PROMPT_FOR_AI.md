# Research Prompt: CS2 Demo Tick Synchronization via Telnet Console

## 🎯 Цель исследования

Найти **все возможные способы** получения текущего тика воспроизведения демо-файла Counter-Strike 2 через telnet консоль (netcon). Текущее решение через команду `demo_info` **не работает** или работает некорректно.

---

## 🔴 Описание проблемы

### Текущая ситуация:
```python
# Подключаемся к CS2 через telnet (порт 2121)
self.writer.write(b"demo_info\n")
response = await self.reader.read(2048)

# Ожидаемый ответ:
# "Demo contents for demo.dem:"
# "Currently playing 12500 of 160000 ticks (0:03:15 / 0:41:40)"
# "at 1.00x speed"

# Парсим через regex:
tick_pattern = re.compile(r"Currently playing (\d+) of \d+ ticks")
match = tick_pattern.search(response_text)
```

### Проблема:
- **Тик всегда остаётся 0** (нуль)
- Либо regex не находит совпадение
- Либо команда `demo_info` не возвращает актуальный тик
- Либо формат ответа CS2 изменился

### Что уже пробовали:
1. ✅ Команда `demo_info` - не работает стабильно
2. ✅ Команда `demo_pause` + `demo_resume` - возвращает неправильный формат
3. ✅ Различные regex patterns - не помогли
4. ✅ Увеличение времени ожидания ответа - не помогло
5. ✅ Проверка кодировки (UTF-8) - в порядке

---

## 📋 Задачи для исследования

### **Часть 1: Проверка команды `demo_info`**

**Вопросы:**
1. Какой **ТОЧНЫЙ** формат ответа команды `demo_info` в Counter-Strike 2 (2024-2025)?
2. Изменился ли формат ответа в последних обновлениях CS2?
3. Есть ли разница между GOTV демо и POV демо?
4. Есть ли разница при воспроизведении через `playdemo` vs `demo_gototick`?
5. Возвращает ли `demo_info` данные **до** начала воспроизведения демо?
6. Нужно ли дождаться какого-то события перед вызовом `demo_info`?

**Где искать:**
- Официальная документация Valve Developer Wiki
- Форумы разработчиков (FaceitFinder, HLTV developers)
- GitHub репозитории с CS2 demo парсерами
- Discord серверы CS2 modding community
- Changelog CS2 (updates.counter-strike.net)

**Что проверить:**
```bash
# В CS2 консоли попробовать:
developer 1
con_logfile console.txt
playdemo demo_name
demo_info  # записать ВЕСЬ вывод
demo_pause
demo_info  # записать вывод снова
demo_resume
demo_gototick 5000
demo_info  # записать вывод снова
```

---

### **Часть 2: Альтернативные команды**

**Исследовать следующие команды:**

1. **`demo_togglepause`** - паузит/продолжает демо
   - Возвращает ли тик в ответе?
   - Формат вывода?

2. **`demo_gototick <tick>`** - переход на конкретный тик
   - Возвращает ли подтверждение с номером тика?
   - Можно ли использовать для синхронизации?

3. **`demo_timescale <speed>`** - изменение скорости воспроизведения
   - Выводит ли текущий тик при изменении?

4. **`getpos`** - позиция игрока
   - Содержит ли информацию о тике?

5. **`status`** - статус сервера
   - Есть ли там информация о тике демо?

6. **`host_timescale`** - скорость игры
   - Связано ли с тиками демо?

7. **`demo_listimportantticks`** - список важных тиков
   - Формат вывода?

8. **`demo_listhighlights`** - список хайлайтов
   - Формат вывода?

**Вопросы:**
- Какие из этих команд доступны через telnet?
- Какие возвращают информацию о текущем тике?
- Какие имеют минимальные побочные эффекты (не паузят, не лагают)?

---

### **Часть 3: ConVars и Events**

**Исследовать переменные (ConVars):**
```
demo_debug               - debug информация о демо
demo_quitafterplayback   - автовыход после демо
demo_interpolateview     - интерполяция вида
cl_showpos               - показ позиции (может включать тик?)
```

**Вопросы:**
1. Есть ли ConVar, который **постоянно выводит** текущий тик в консоль?
2. Можно ли включить verbose режим для demo playback?
3. Есть ли способ подписаться на события смены тика?
4. Можно ли получать обновления тика автоматически (push), а не через polling?

**Где искать:**
- `find / -type f -name "*.cfg" 2>/dev/null | xargs grep demo`
- Декомпиляция `client.dll` / `server.dll`
- Source 2 SDK (если доступен)
- Leaked source code (GitHub)

---

### **Часть 4: Альтернативные протоколы**

**Исследовать:**

1. **RCON (Remote Console):**
   - Отличается ли от telnet netcon?
   - Есть ли дополнительные команды?
   - Более стабильный ли протокол?

2. **Steam API:**
   - Можно ли получить информацию о демо через Steam API?
   - `ISteamMatchmakingServers` interface?

3. **Game State Integration (GSI):**
   - Работает ли GSI с demo playback?
   - Передаёт ли текущий тик?
   - Конфигурация для demo mode?

4. **Shared Memory:**
   - Использует ли CS2 shared memory для IPC?
   - Можно ли читать из него текущий тик?

5. **Log Files:**
   - Пишет ли CS2 текущий тик в `console.log`?
   - Можно ли использовать `con_logfile` + tail -f?
   - Формат логов?

6. **Network Sniffing:**
   - Можно ли перехватить пакеты localhost telnet?
   - Скрытые команды или параметры?

---

### **Часть 5: Source Engine Research**

**Изучить Source 2 Engine:**

1. **Demo File Format:**
   - Структура `.dem` файла
   - Header содержит tickrate?
   - Можно ли читать тик напрямую из файла во время воспроизведения?

2. **Command Buffer:**
   - Как CS2 обрабатывает команды из telnet?
   - Очередь команд?
   - Синхронные vs асинхронные команды?

3. **Demo Player Implementation:**
   - `CDemoPlayer` класс
   - Методы получения текущего тика
   - Internal state machine

**Где искать:**
- Source SDK 2013 (похожая архитектура)
- Valve Developer Wiki
- cs2-dumper (GitHub) - дампы классов CS2
- IDA/Ghidra дизассемблер для `engine2.dll`

---

### **Часть 6: Community Solutions**

**Найти существующие решения:**

1. **GitHub repositories:**
   - Поиск: "CS2 demo tick" OR "CS2 demo parser" OR "CS2 telnet"
   - Языки: Python, JavaScript, C++, C#
   - Проекты аналитики демо, бот-системы, статистика

2. **Steam Community:**
   - Форум CS2 Developer
   - Группы modding'а

3. **Reddit:**
   - r/cs2 - поиск "demo tick"
   - r/sourceengine
   - r/csmapmakers

4. **Discord серверы:**
   - CS2 Modding Community
   - FaceIt Developer
   - HLTV Developer

**Вопросы к сообществу:**
- Кто-то уже решал эту проблему?
- Есть ли готовые библиотеки?
- Какие workarounds используют другие?

---

### **Часть 7: Reverse Engineering**

**Если ничего не помогло:**

1. **Wireshark packet capture:**
   ```bash
   wireshark -i lo -f "tcp port 2121"
   # Воспроизвести демо вручную
   # Записать ВСЕ пакеты telnet
   # Найти скрытые команды или форматы
   ```

2. **Process Monitor:**
   - Отследить все системные вызовы CS2 во время `demo_info`
   - Найти откуда берётся информация о тике

3. **Memory Dump:**
   - Найти адрес переменной current_tick в памяти
   - Читать напрямую через external process (VAC-unsafe!)

4. **String Search in Binaries:**
   ```bash
   strings engine2.dll | grep -i "currently playing\|demo tick\|playback"
   strings client.dll | grep -i "demo\|tick"
   ```

---

## 📊 Требуемый формат ответа

### Для КАЖДОГО найденного способа предоставить:

```markdown
### Способ N: [Название]

**Команда/Метод:**
```bash
<точная команда или код>
```

**Ожидаемый ответ:**
```
<пример реального вывода>
```

**Regex для парсинга:**
```python
pattern = re.compile(r"...")
```

**Плюсы:**
- ...

**Минусы:**
- ...

**Надёжность:** [1-10]
**Производительность:** [1-10]
**Сложность реализации:** [1-10]

**Тестирование:**
- [ ] Протестировано на CS2 версия XXXX
- [ ] Работает с GOTV демо
- [ ] Работает с POV демо
- [ ] Работает при паузе
- [ ] Работает при перемотке

**Источник:**
[Ссылка на документацию/форум/код]
```

---

## 🎯 Приоритеты решений

**Критерии оценки (в порядке важности):**

1. **Надёжность** (10/10 приоритет):
   - Работает 100% времени
   - Не зависит от версии CS2
   - Не ломается при обновлениях

2. **Производительность** (9/10):
   - Минимальная задержка (<10ms)
   - Не лагает демо воспроизведение
   - Можно опрашивать 60+ раз в секунду

3. **Простота** (7/10):
   - Простой код
   - Не требует root/admin
   - Работает на Windows/Linux

4. **Безопасность** (10/10):
   - VAC-safe
   - Не читает память процесса
   - Не модифицирует файлы

---

## 📚 Рекомендуемые источники

### Обязательно проверить:

1. **Valve Developer Community Wiki:**
   - https://developer.valvesoftware.com/wiki/Demo_Recording_Tools
   - https://developer.valvesoftware.com/wiki/Command_Line_Options
   - https://developer.valvesoftware.com/wiki/Console_Command_List

2. **CS2 GitHub Projects:**
   - https://github.com/search?q=cs2+demo+parser
   - https://github.com/search?q=counter-strike+2+telnet
   - https://github.com/a2x/cs2-dumper (offsets and classes)

3. **Reddit Threads:**
   - https://www.reddit.com/r/cs2/search?q=demo+tick
   - https://www.reddit.com/r/sourceengine/

4. **Steam Community:**
   - https://steamcommunity.com/app/730/discussions/

5. **HLTV Developer Forum:**
   - https://www.hltv.org/forums/

---

## 🔬 Примеры для тестирования

### Test Case 1: Базовая команда
```python
import asyncio

async def test_demo_info():
    reader, writer = await asyncio.open_connection('127.0.0.1', 2121)
    writer.write(b"demo_info\n")
    await writer.drain()
    response = await asyncio.wait_for(reader.read(4096), timeout=2.0)
    print("RAW RESPONSE:")
    print(repr(response))
    print("\nDECODED:")
    print(response.decode('utf-8', errors='ignore'))
    writer.close()
    await writer.wait_closed()

asyncio.run(test_demo_info())
```

### Test Case 2: Альтернативные команды
```python
commands = [
    b"demo_info\n",
    b"demo_pause\n",
    b"status\n",
    b"host_timescale\n",
    b"demo_listimportantticks\n",
]

for cmd in commands:
    print(f"\n=== Testing: {cmd.decode().strip()} ===")
    # ... отправить и вывести ответ
```

---

## ✅ Критерии успешного решения

Решение считается найденным, если:

1. ✅ Возвращает **актуальный текущий тик** (не 0, не устаревший)
2. ✅ Работает **стабильно** (99%+ success rate)
3. ✅ Работает **в реальном времени** (задержка <50ms)
4. ✅ Можно использовать для **60 FPS синхронизации**
5. ✅ **Не требует** паузы демо
6. ✅ **VAC-safe** (не читает память)
7. ✅ Работает через **telnet/netcon** (или другой легальный API)

---

## 💡 Дополнительные направления

Если прямого решения нет, исследовать:

1. **Гибридный подход:**
   - Использовать timestamp + tickrate для предсказания
   - Периодическая валидация через косвенные методы

2. **Парсинг лог-файлов:**
   - `con_logfile console.txt`
   - Real-time tail -f
   - Regex на события смены тика

3. **Proxy подход:**
   - Создать локальный прокси сервер
   - Перехватывать команды между CS2 и telnet
   - Инжектить дополнительные запросы

4. **File watching:**
   - Мониторить изменения в demo файле во время воспроизведения
   - Определять позицию по file offset

---

## 📝 Формат финального отчёта

Предоставить:

1. **Executive Summary** (1 страница):
   - Лучшее найденное решение
   - Почему оно работает
   - Как использовать

2. **Детальный анализ** (5-10 страниц):
   - Все протестированные методы
   - Результаты тестирования
   - Сравнительная таблица

3. **Код примеров:**
   - Working implementation
   - Unit tests
   - Integration test

4. **Troubleshooting guide:**
   - Частые проблемы
   - Решения
   - FAQ

---

## ⚡ Начни исследование СЕЙЧАС!

**Первые 5 шагов:**

1. Запусти CS2 с `-netconport 2121 -insecure`
2. Загрузи любую демку: `playdemo demo_name`
3. Подключись через telnet и отправь `demo_info`
4. Сохрани **ВЕСЬ** raw output в файл
5. Проанализируй каждую строку вывода

**Удачи в исследовании! 🚀**

---

*Этот промпт создан для детального исследования проблемы синхронизации тиков CS2 через telnet console. Используй его для поиска альтернативных решений.*
