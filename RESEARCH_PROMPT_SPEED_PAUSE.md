# Research Prompt: CS2 Demo Playback Speed & Pause Detection

## 🎯 Цель исследования

Найти **все способы** определения:
1. **Текущей скорости воспроизведения демо** (1.0x, 0.25x, 2.0x и т.д.)
2. **Находится ли демо на паузе** (точная детекция)

Через **telnet console (netcon)** в Counter-Strike 2.

---

## 🔴 Контекст проблемы

### Что мы УЖЕ знаем:

**Работает:**
- ✅ `demo_marktick` - возвращает текущий тик (файловый)
- ✅ GUI консоль показывает `CGameRules - paused on tick XXX` при паузе

**НЕ работает:**
- ❌ `con_logfile` - команда не существует в CS2
- ❌ `demo_info` - не возвращает информацию о тике/скорости

### Что нам НУЖНО:

1. **Узнать скорость воспроизведения:**
   - Пользователь может изменить скорость (demo_timescale 0.25 / 0.5 / 2.0)
   - Нужно детектить изменение скорости в реальном времени
   - Иначе prediction будет неточным

2. **Точно детектить паузу:**
   - Отличить паузу от очень медленной скорости (0.1x)
   - Узнать СРАЗУ когда пользователь нажал паузу (не через 3 секунды)
   - Нужно для заморозки overlay

---

## 📋 Задачи исследования

### **Часть 1: Команды для скорости воспроизведения**

**Исследовать команды:**

1. **demo_timescale**
   ```
   Вопросы:
   - Как УСТАНОВИТЬ скорость? demo_timescale 0.25
   - Как УЗНАТЬ текущую скорость? demo_timescale без параметров?
   - Что возвращает через telnet?
   - Формат ответа?
   ```

2. **host_timescale**
   ```
   Вопросы:
   - Связана ли с demo_timescale?
   - Возвращает ли текущую скорость?
   - Работает ли при воспроизведении демо?
   ```

3. **demo_speed / demo_playback_speed**
   ```
   Вопросы:
   - Существуют ли такие команды?
   - Альтернативные названия?
   ```

4. **Любые команды с "speed" / "timescale" / "rate"**
   ```
   Задача:
   - Найти ВСЕ команды связанные со скоростью
   - Протестировать каждую через telnet
   ```

**Где искать:**
- CS2 CVARs list: https://gist.github.com/dgibbs64/6c321d87004c96919fccd6163ca29d90
- Valve Developer Wiki
- GitHub: поиск "cs2 demo timescale"
- Community forums

---

### **Часть 2: Команды для детекции паузы**

**Исследовать:**

1. **demo_pause + telnet response**
   ```python
   # КРИТИЧНО: Что ТОЧНО возвращает telnet на demo_pause?

   import asyncio

   async def test():
       reader, writer = await asyncio.open_connection('127.0.0.1', 2121)

       # Отправить demo_pause
       writer.write(b"demo_pause\n")
       await writer.drain()

       # Прочитать ответ
       response = await asyncio.wait_for(reader.read(4096), timeout=2.0)

       print("=== RAW RESPONSE ===")
       print(repr(response))
       print("\n=== DECODED ===")
       print(response.decode('utf-8', errors='ignore'))

   asyncio.run(test())
   ```

   **Вопросы:**
   - Содержит ли ответ "CGameRules - paused on tick XXX"?
   - Или только "Demo paused"?
   - Или вообще ничего?

2. **demo_resume + telnet response**
   ```
   Аналогично - что возвращает?
   - "CGameRules - unpaused on tick XXX"?
   - Информация о длительности паузы?
   ```

3. **demo_togglepause**
   ```
   Вопросы:
   - Переключает паузу (пауза ↔ воспроизведение)
   - Что возвращает через telnet?
   - Есть ли информация о текущем состоянии?
   ```

4. **Команды для ЗАПРОСА состояния (без изменения)**
   ```
   Вопросы:
   - Есть ли команда типа "demo_status" или "demo_state"?
   - Что-то вроде "demo_info" но работающее?
   - Команда которая возвращает: paused=true/false, speed=0.25, tick=39500
   ```

---

### **Часть 3: Вычисление из данных**

**Если прямых команд нет, исследовать:**

1. **Точность вычисления скорости из истории тиков**
   ```python
   # Метод:
   history = [
       (time=0.0, tick=39500),
       (time=0.5, tick=39532),  # +32 тика за 0.5с
   ]

   # Вычисление:
   tick_diff = 39532 - 39500 = 32
   time_diff = 0.5 - 0.0 = 0.5
   rate = 32 / 0.5 = 64 ticks/sec
   speed = 64 / 64 (tickrate) = 1.0x

   # Вопросы:
   # - Насколько точно это работает?
   # - Минимум измерений для стабильного определения?
   # - Как быстро адаптируется при смене скорости?
   ```

2. **Детекция паузы из истории**
   ```python
   # Метод: последние N синхронизаций вернули одинаковый тик
   if all(tick == same_value for tick in last_3_ticks) and same_value > 0:
       is_paused = True

   # Вопросы:
   # - Сколько N нужно для уверенности? (3? 5?)
   # - Как быстро детектится? (N * sync_interval)
   # - Можно ли отличить паузу от очень низкой скорости (0.05x)?
   ```

3. **Отличие паузы от медленной скорости**
   ```python
   # При скорости 0.1x:
   # За 0.5 сек пройдёт: 64 * 0.5 * 0.1 = 3.2 ≈ 3 тика

   # При паузе:
   # За 0.5 сек пройдёт: 0 тиков

   # Но если sync_interval = 0.5s:
   # - Разница 0 тиков = пауза ИЛИ скорость <0.03x
   # - Как точно определить?

   # Решение: если несколько раз подряд 0 тиков = пауза
   ```

---

### **Часть 4: Альтернативные источники информации**

**Исследовать:**

1. **UI Overlay Elements**
   ```
   Вопросы:
   - Рендерит ли CS2 текст скорости на экране?
   - Можно ли получить через accessibility API?
   - OCR как последний resort?
   ```

2. **Memory Reading (VAC-unsafe, но для информации)**
   ```
   Вопросы:
   - Есть ли оффсеты для demo playback speed?
   - Адрес переменной is_paused?
   - cs2-dumper проект на GitHub
   ```

3. **Network packets analysis**
   ```
   Вопросы:
   - Wireshark на localhost:2121
   - Скрытые команды или параметры?
   - Бинарный протокол вместо текста?
   ```

4. **Keyboard hooks (последний resort)**
   ```
   Вопросы:
   - Можно ли перехватить нажатие клавиши паузы?
   - Хоткеи CS2 для demo_timescale?
   - Программный способ детекта без чтения памяти?
   ```

---

## 📊 Требуемый формат ответа

### Для КАЖДОГО метода:

```markdown
### Метод N: [Название]

**Команда/Способ:**
```bash
<точная команда>
```

**Ожидаемый ответ (через telnet):**
```
<реальный вывод CS2>
```

**Пример парсинга:**
```python
# Python код для извлечения информации
pattern = re.compile(r"...")
match = pattern.search(response)
speed = float(match.group(1))
```

**Как определить:**
- Скорость: [Да/Нет, формат]
- Паузу: [Да/Нет, формат]

**Плюсы:**
- ...

**Минусы:**
- ...

**Точность:** [1-10]
**Задержка:** [мс]
**Сложность:** [1-10]

**Тестирование:**
- [ ] Работает на CS2 версия 2024+
- [ ] Детектит скорость 0.25x, 0.5x, 1.0x, 2.0x
- [ ] Детектит паузу
- [ ] Работает через telnet

**Источник:**
[Ссылка]
```

---

## 🎯 Конкретные тесты

### Тест 1: demo_timescale

```python
import asyncio

async def test_timescale():
    reader, writer = await asyncio.open_connection('127.0.0.1', 2121)

    # Test 1: Установить скорость
    print("=== Setting speed to 0.25x ===")
    writer.write(b"demo_timescale 0.25\n")
    await writer.drain()
    response = await asyncio.wait_for(reader.read(2048), timeout=1.0)
    print(response.decode('utf-8', errors='ignore'))

    await asyncio.sleep(1)

    # Test 2: Запросить текущую скорость
    print("\n=== Querying current speed ===")
    writer.write(b"demo_timescale\n")
    await writer.drain()
    response = await asyncio.wait_for(reader.read(2048), timeout=1.0)
    print(response.decode('utf-8', errors='ignore'))

    writer.close()
    await writer.wait_closed()

asyncio.run(test_timescale())
```

**Запусти это и дай мне ВЕСЬ вывод!**

---

### Тест 2: demo_pause response

```python
async def test_pause():
    reader, writer = await asyncio.open_connection('127.0.0.1', 2121)

    print("=== Sending demo_pause ===")
    writer.write(b"demo_pause\n")
    await writer.drain()

    # Прочитать всё что вернулось
    response = await asyncio.wait_for(reader.read(4096), timeout=2.0)

    print("RAW bytes:")
    print(repr(response))
    print("\nDecoded:")
    print(response.decode('utf-8', errors='ignore'))
    print("\nBytes length:", len(response))

    writer.close()
    await writer.wait_closed()

asyncio.run(test_pause())
```

**Запусти это и дай мне ВЕСЬ вывод!**

---

### Тест 3: Все demo_* команды

```bash
# В CS2 консоли:
find demo_

# Или через telnet:
help demo
find demo
```

**Скинь список ВСЕХ команд начинающихся с demo_**

---

## 💡 Приоритеты решений

**Оценка по критериям:**

1. **Точность** (10/10 важность):
   - Скорость определяется с точностью ±0.05x
   - Пауза детектится мгновенно (<100ms)

2. **Задержка** (9/10):
   - Детекция изменения скорости <1 секунды
   - Детекция паузы <500ms

3. **Надёжность** (10/10):
   - Работает при любых скоростях (0.1x - 5.0x)
   - Не ломается при прыжках (Shift+F2)

4. **Простота** (6/10):
   - Код понятный и поддерживаемый
   - Не требует сложной математики

---

## 🔬 Сценарии для тестирования

**Сценарий 1: Изменение скорости**
```
1. Начать воспроизведение на 1.0x
2. Через 5 секунд → 0.25x
3. Через 5 секунд → 2.0x
4. Проверить: как быстро система адаптируется?
```

**Сценарий 2: Пауза и возобновление**
```
1. Воспроизведение на 1.0x
2. Пауза на 3 секунды
3. Возобновление
4. Проверить: детектится ли пауза? С какой задержкой?
```

**Сценарий 3: Очень медленная скорость**
```
1. Установить 0.05x
2. Проверить: спутается ли с паузой?
```

**Сценарий 4: Прыжок во время паузы**
```
1. Пауза
2. Shift+F2 на другой тик
3. Возобновление
4. Проверить: корректно ли обработается?
```

---

## ✅ Критерии успешного решения

Решение найдено если:

1. ✅ Можем **узнать** текущую скорость воспроизведения
2. ✅ Можем **детектить изменение** скорости быстро (<1 сек)
3. ✅ Можем **точно отличить** паузу от медленной скорости
4. ✅ Детекция паузы **мгновенная** (<500ms)
5. ✅ Работает через **telnet** (или другой легальный метод)
6. ✅ **VAC-safe** (не читает память, не модифицирует файлы)

---

## 📚 Источники для исследования

**Обязательно проверить:**

1. **CS2 CVARs List:**
   - https://gist.github.com/dgibbs64/6c321d87004c96919fccd6163ca29d90
   - https://wiki.ckras.com/wiki/index.php?title=CS2_CVARs

2. **Demo commands documentation:**
   - https://cs2pulse.com/cs2-replay-commands-guide/
   - https://bo3.gg/articles/cs2-demo-controls-and-commands-recording-and-playback

3. **Source Engine internals:**
   - https://developer.valvesoftware.com/wiki/Demo_Recording_Tools
   - https://github.com/a2x/cs2-dumper (memory offsets)

4. **Community projects:**
   - GitHub search: "cs2 demo playback speed"
   - GitHub search: "cs2 telnet demo control"

---

## 🎯 НАЧНИ ИССЛЕДОВАНИЕ СЕЙЧАС!

**Первые действия:**

1. Запусти CS2 с `-netconport 2121 -insecure`
2. Загрузи демо: `playdemo demo_name`
3. Подключись через telnet: `telnet localhost 2121`
4. Выполни тесты 1, 2, 3 выше
5. Скинь МНЕ все выводы (каждый байт!)

**Особенно важно:**
- Что возвращает `demo_timescale`?
- Что возвращает `demo_pause` через telnet?
- Список всех `demo_*` команд

---

## 💬 Формат финального отчёта

Предоставь:

1. **Executive Summary** (1 страница):
   - Лучший способ узнать скорость
   - Лучший способ детектить паузу
   - Код примера

2. **Детальный анализ** (5-10 страниц):
   - Все протестированные команды
   - Результаты каждого теста
   - Сравнительная таблица

3. **Working Implementation:**
   - Python класс для определения скорости и паузы
   - Unit tests
   - Примеры использования

4. **Edge cases:**
   - Очень медленная скорость (0.05x)
   - Очень быстрая скорость (5.0x)
   - Пауза + прыжок
   - Смена скорости во время паузы

---

**Удачи в исследовании! 🚀**

*Этот промпт создан для поиска методов определения скорости воспроизведения и детекции паузы в CS2 через telnet.*
